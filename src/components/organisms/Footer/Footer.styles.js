import styled from "styled-components";
import { Row } from "react-bootstrap";
import { colorsPallete } from "theme/variables";

export const FooterRow = styled(Row)`
    height: 70px;
    background-color: ${colorsPallete.capeCod};
`;